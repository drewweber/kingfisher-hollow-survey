{
  "name": "kingfisher-field-alerts",
  "private": true,
  "type": "module",
  "scripts": {
    "build:known": "python3 scripts/build-known-moths.py",
    "test": "node --test test/*.test.mjs",
    "dev": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler dev --test-scheduled",
    "deploy": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

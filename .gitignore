# Python
.venv/
__pycache__/
*.pyc

# Local runtime artifacts
logs/
node_modules/

# Generated report; deployed via wrangler, not committed.
public/

# SQLite database — persisted between cloud runs via GitHub Actions cache,
# rebuilt from the iNat API on a cache miss (idempotent). Not stored in git.
data/inat.db
data/inat.db-wal
data/inat.db-shm
data/cache/

# Original CSV exports — superseded by the live API; kept locally as an archive.
*.csv
!data/ebird_L41961519_life_list.csv

# Wrangler cache — contains Cloudflare account ID; not needed in version control.
.wrangler/
field-alerts/.dev.vars
field-alerts/node_modules/
